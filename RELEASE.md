# 출시 준비 상태

> 이 문서는 **코드를 기준으로** 적었다 (2026-08-31).
> 저장소 안에서 끝낼 수 있는 일은 끝냈고, 계정·자격·법률 검토가 필요한 일은 무엇이 왜 막혀 있는지 적었다.

## 1. 저장소에서 끝난 것

| 항목 | 상태 |
|------|------|
| 버전 | `1.0.0+1` (`pubspec.yaml`) — 첫 출시 기준 |
| Android 서명 | `android/key.properties` + `upload-keystore.jks`로 릴리스 서명이 물린다. 둘 다 gitignore 대상이라 저장소에는 없다 |
| Android 출시 산출물 | `build/app/outputs/bundle/release/app-release.aab` (88.8MB) — 릴리스 키(`CN=Foxtrot`, 2053년까지)로 서명된 것을 확인했다 |
| iOS 빌드 | `flutter build ios --no-codesign` 통과. 서명은 계정이 붙어야 한다 |
| 웹 빌드 | `flutter build web --release` 통과 |
| 앱 아이콘 | Android(`mipmap-*`) · iOS(`AppIcon.appiconset`) 모두 들어 있다 |
| 표시 이름 | 양쪽 다 **폭스트롯**. iOS가 `Cafe App`으로 뜨던 것을 고쳤다 |
| 최소 OS | Android API 23 / iOS 15.0 (현재 Firebase iOS SDK가 15.0 미만을 지원하지 않는다) |
| 약관·개인정보처리방침 | 앱 안에 있고 **로그인 없이 열린다**. 스토어 심사가 요구하는 외부 주소로 쓸 수 있다 |
| 서버 | Firestore 규칙 · Cloud Functions 19개 · Remote Config 4종이 운영(`foxtrot-3bdba`)에 올라가 있다 |
| CI | 포맷·분석·테스트(559) · 함수(180) · 보안 규칙(44) · Android 빌드 |

### 릴리스 서명 지문

`upload-keystore.jks`로 서명한 빌드의 지문이다. 비밀이 아니라 각 콘솔에 등록하는 값이다.

| 용도 | 값 |
|------|-----|
| SHA-1 | `30:AE:0F:23:DC:55:A9:C2:96:62:6F:2C:4B:AD:76:17:4D:93:4F:D7` |
| SHA-256 | `09:45:0F:9E:D6:5C:87:60:55:2C:FC:C6:43:19:B6:74:84:7A:93:13:FD:34:1D:59:F8:49:07:8A:2F:71:99:E9` |
| 카카오 키 해시 | `MK4PI9xVqcKWYm8sS612F02TT9c=` |

- **Firebase**: 릴리스 SHA-1·SHA-256과 디버그 SHA-1을 등록했다. 등록 전에는 Android용 OAuth 클라이언트가 아예 없어 **구글 로그인이 `DEVELOPER_ERROR`로 실패하는 상태**였다. `android/app/google-services.json`도 갱신본으로 교체했다
- **카카오 개발자 콘솔**: 위 키 해시를 등록해야 릴리스 빌드에서 카카오 로그인이 된다 (§3)
- **네이버 개발자 콘솔**: 릴리스 빌드의 패키지명·서명 지문을 확인해야 한다 (§3)
- Play 앱 서명을 켜면 Google이 최종 서명 키를 따로 발급하므로, **등록 후 Play Console이 알려 주는 지문도 같은 콘솔들에 추가**해야 한다

### 방침 주소로 쓸 수 있는 곳

웹을 호스팅에 올리면 아래 주소가 로그인 없이 열린다. 스토어 양식의 "개인정보처리방침 URL"에 그대로 넣을 수 있다.

- 이용약관: `https://<호스팅 도메인>/#/profile/terms`
- 개인정보처리방침: `https://<호스팅 도메인>/#/profile/privacy`

**웹 호스팅 배포가 선행되어야 한다** (§3 참고).

## 2. 스토어 양식에 적을 값

코드가 실제로 무엇을 다루는지 정리한 것이다. Play 데이터 보안(Data safety)과 App Store 개인정보 라벨은 **이 표와 개인정보처리방침이 서로 어긋나면 반려된다.**

| 데이터 | 수집 여부 | 쓰이는 곳 | 계정과 연결 |
|--------|-----------|-----------|-------------|
| 이름·닉네임, 이메일 | 수집 | 소셜 로그인(카카오/네이버/구글/애플) | 예 |
| 소셜 로그인 식별자 | 수집 | 회원 식별 | 예 |
| 전화번호·주소 | 수집(선택) | 원두 배송지, 선물 받는 사람 | 예 |
| 대략적 위치 | 수집 | 매장 찾기 거리 정렬 (`geolocator`) | 아니오 |
| 결제 정보 | **직접 저장하지 않음** | 토스페이먼츠 결제창이 처리하고, 앱은 결제 키·수단 별칭만 보관 | 예 |
| 사진 | 수집 | 상품 사진 등록 — **매장 관리자 계정만** | 예 |
| 카메라 | 사용 | 회원 QR 스캔 적립 (직원 화면) | 아니오 |
| 기기 식별자(FCM 토큰) | 수집 | 주문 상태·포인트 푸시 | 예 |
| 구매 내역 | 수집 | 주문 내역, 포인트 적립 | 예 |

### ⚠️ 개인정보처리방침과 코드가 어긋난다

`lib/features/profile/presentation/policy_screen.dart`의 "1. 수집하는 개인정보 항목"은 **이름·이메일·소셜 식별자·프로필 사진·배송지**만 적고 있다. 코드는 여기에 더해 **위치, FCM 토큰, 결제 식별자, 카메라**를 다룬다.

이대로 제출하면 방침과 스토어 신고가 어긋난다. **법률 문서라 임의로 고치지 않았다.** 아래 문구를 검토해 넣을지 정하면 반영한다.

> - 자동 수집: 서비스 이용 기록, 기기 식별자(푸시 알림 토큰)
> - 위치 정보: 가까운 매장을 찾을 때에 한해 기기의 위치를 사용하며, 서버에 저장하지 않습니다
> - 결제 정보: 카드 번호 등 결제 수단 정보는 결제대행사(토스페이먼츠)가 처리하며, 회사는 결제 식별자와 수단 별칭만 보관합니다

## 3. 계정·자격이 있어야 되는 일

순서대로 두면 이렇다.

1. **웹 호스팅 배포** — `flutter build web --release && firebase deploy --only hosting --project foxtrot-3bdba`
   방침 주소가 여기서 나오므로 스토어 양식보다 먼저 해야 한다. 지금은 서버(규칙·함수·설정)만 올라가 있고 웹 앱은 옛 버전이다.
2. **카카오·네이버 콘솔에 릴리스 키 등록** — 위 키 해시·지문. 등록하지 않으면 릴리스 빌드에서 소셜 로그인이 실패한다. Play 앱 서명을 쓰면 Play가 발급한 지문도 함께 등록해야 한다.
3. **토스페이먼츠 운영 키** — 발급 후 빌드에 `--dart-define=TOSS_CLIENT_KEY=...`, 서버 시크릿에 시크릿 키.
   **지금은 테스트 키로 동작한다.** 이대로 출시하면 실제 결제가 되지 않는다.
4. **Google Play Console** — 앱 등록, `.aab` 업로드, 스토어 등록정보, 데이터 보안 양식(§2), 콘텐츠 등급.
5. **App Store Connect + Apple Developer** — 앱 레코드 생성, 서명 인증서·프로비저닝, `DEVELOPMENT_TEAM` 설정(현재 비어 있어 아카이브가 되지 않는다), 개인정보 라벨(§2).
6. **약관·방침 영문본** — 영어 UI가 있어 영어권 심사에서 요구될 수 있다. 법률 검토를 거친 번역문이 필요하다.
7. **`store_url` 채우기** — 스토어 등재 후 `remoteconfig.template.json`에 android/ios 조건을 붙인다. 지금은 비어 있어 강제 업데이트 안내의 이동 버튼이 숨는다.
8. **`min_supported_version`** — 출시 후 강제 업데이트가 필요해질 때 올린다. 지금 비워 두면 아무도 막지 않는다.

## 4. 출시 전에 판단이 필요한 것

- **포인트 사용 잠금이 기본 켜짐이다.** 이미 쓰던 사람도 다음 포인트 사용에서 처음으로 기기 잠금 확인을 만난다. 배포 안내에 한 줄 적는 편이 좋다 (`/profile/security`에서 끌 수 있다).
- **iOS 13·14 기기가 빠진다.** Firebase SDK 요구사항이라 선택지가 없었다.
- **R8/난독화를 켜지 않았다.** 켜면 용량이 줄지만 카카오·네이버 SDK가 리플렉션을 쓰므로 실기기 확인 없이 켜는 것은 위험하다.
- **App Check을 쓰지 않는다.** 콜러블 함수는 로그인 검사만 지난다. 남용이 걱정되면 출시 후 도입을 검토한다.

## 5. 실기기에서 한 번은 봐야 하는 것

자동 테스트가 닿지 않는 것들이다.

- 카카오·네이버 로그인 — **서명 키가 바뀌면 키 해시가 달라진다.** 릴리스 키로 서명한 빌드로 확인해야 한다
- 푸시 수신 (주문 상태, 포인트)
- **포인트 사용 잠금** — 생체 인증은 에뮬레이터·시뮬레이터에서 실제 동작을 확인하기 어렵다
- 토스 결제창 (운영 키를 넣은 뒤)
